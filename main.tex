% This is a template for your written document.
%
% To compile using latexmk on the command line, run the following: 
% latexmk -pdf main.tex

\documentclass[12pt]{article}
\usepackage{setspace}
\singlespace
\usepackage[left=1in,right=1in,top=1in,bottom=1in]{geometry}
\usepackage{graphicx}
\usepackage{float}
\usepackage{amsmath}

\title{\textbf{A Phase Vocoder Approach To Pitch Shifting}}
\author{Sam Smith}

\begin{document}

\maketitle

\section{Introduction}
%The introduction will broadly introduce your topic, motivate it, and describe what will be done.
The manipulation of pitch in digital signals is not achieved by simply scaling frequencies, 
but through structured transformations in the time-frequency domain.

\section{Background}

\subsection{Discrete Fourier Transform}
The Discrete Fourier Transform is a fundamental tool used in digital signal processing that 
enables sophisticated analysis and synthesis of discrete-time signals \cite{lyons2010understanding}.   
The DFT transforms a signal from the time domain into its frequency-domain representation,  
allowing the signal to be expressed as a sum of sinusoids \cite{lyons2010understanding}. Mathematically, the DFT is defined as: 
\begin{align}
  X[m] = \sum_{n=0}^{N-1} x[n] \, e^{-j 2\pi n\frac{m}{N}}
\end{align}
where $N$ is the number of samples from the input signal $x[n]$, $n$ denotes the time-domain index, $m$ represents the frequency bin index, 
$n$ is the index of the input sequenc in the time-domain and $j = \sqrt{-1}$. Each coeffecient in $X[m]$ is complex-valued and can be 
expressed in terms of magnitude and phase, which describe the contribution of a corresponding frequency component to the overall signal \cite{mcfee_dstbook}.

\subsection{Windowing}
Windowing is a function used to isolate parts of a discrete time signal so that analysis on the signal can be performed on contiguous blocks \cite{barry2008time}.
More specifically, the input signal is multiplied by a windowing function whose domain is nonzero for a particular range. This results in a small localized snapshot of the input signal,
and every other value outside of the windows domain is reduced to zero \cite{royer2019pitch}. Figure 1 below demonstrates the functionality of a window function. 

\begin{figure}[H] 
\begin{center}
  \includegraphics[width=8cm]{imgs/non-windowed.png}
  \includegraphics[width=8cm]{imgs/windowed.png}
\caption{After applying a window function, the signal is tapered at the edges, resulting in a temporally localized signal.} 
 \label{fig:non-windowed-and-windowed}
 \end{center}
\end{figure}

As dipicted by Figure~\ref{fig:non-windowed-and-windowed}, after applying the window function to the example signal, the edges of the signal
tend toward zero. In this example, the Hanning window function was used to shape the signal, however, there are multiple windowing functions
such as the Rectangle window function, Triangle window function and Hamming window function \cite{mcfee_dstbook}

\subsection{Short-Time Fourier Transform}
The Short-Time Fourier Transform (STFT) is a time-frequency representation used to compute the DFT of contiguous windowed segments of a signal. 
This is accomplished by multiplying the signal by a windowing function so that only a small portion of the signal is nonzero \cite{dolson1986phase}. 
Mathematically, it is similar to equation $(1)$ but with a few modifications: the addition of a windowing function, the domain of the input signal, 
and how the output is interpreted. Below is the STFT: 
\begin{align}
  X[m,k] = \sum_{n=0}^{N-1} x[n+kH]w[n]e^{-2\pi jmn/N}
\end{align}
Where $X[m, k]$ is the 2-dimensional output array indexed by both frequency bin and time frame and $x[n + kH]$ is the section of the input signal 
determined by frame index and hop size \cite{royer2019pitch}. 


\section{Related Works}

\section{Methodology}
Dijkstra's algorithm was chosen for single-source shortest path computation on graphs with non-negative edge weights, following the implementation presented in CLRS~\cite{clrsAlgorithms}.

\section{Results}
An exemplary figure is shown in Figure~\ref{fig:dijkstra}. Any time a figure is used, it must have a capation and be directly referred to and discussed in the text.

%\begin{figure}[b] puts the image to the (closest) bottom of page
%\begin{figure}[t] puts the image to the (closest) top of page
%\begin{figure}[h] allows the image to float, putting it roughly wherever it was placed in the text.
% \begin{figure}[b] 
% \begin{center}
%   \includegraphics[width=5cm]{imgs/dijkstra.png}
%   \caption{Dijkstra's algorithm does not work on graphs with negative edge weights, as evidenced here.}
%   \label{fig:dijkstra}
%  \end{center}
% \end{figure}



\section{Conclusion}

\bibliographystyle{acm}
\bibliography{bibliography.bib}

\end{document}
